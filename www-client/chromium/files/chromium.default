# Default settings for chromium. This file is sourced by /bin/bash from
# the chromium launcher.

# Options to pass to chromium.
CHROMIUM_FLAGS="--enable-native-gpu-memory-buffers --password-store=basic --disable-touch-adjustment --enable-gpu-rasterization --enable-tcp-fastopen --ignore-gpu-blacklist --touch-events=disabled --disable-features=enable-automatic-password-saving,enable-password-force-saving"
